# Copy this to .env when running with docker-compose.
# Generate APP_KEY with: php artisan key:generate --show
APP_KEY=base64:REPLACE_WITH_GENERATED_KEY

# All other values are set directly in docker-compose.yml.
# Override them here if needed for your local setup.
